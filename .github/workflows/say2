name: say2
descripcion: to say something
inputs:
  msg:
    required: true
    type: string
    default: hello people
jobs:
  say:
    runs-on: ubuntu-latest

    steps:
    - run: echo "${{ inputs.name }}"
