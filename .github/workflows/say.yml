name: say
on:
  workflow_dispatch:
    inputs:
      name:
        required: true
        type: string
        default: julius
jobs:
  say:
    runs-on: ubuntu-latest

    steps:
    - run: echo "${{ github.event.inputs.name }}"
