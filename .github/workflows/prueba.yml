name: CI
on:
  push:
jobs:
  upload:
    runs-on: ubuntu-latest

    steps:
      - run: pip install boto3
      - run: npm install @aws-sdk/client-s3
      - run: go get -u github.com/aws/aws-sdk-go
      - run: composer require aws/aws-sdk-php
      - run: |
          cat >prueba.py <<EOF
          import sys
          
          print(sys.argv[0])
          print(sys.argv[1])

          EOF
          cat prueba.py
          python prueba.py hola
